<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.suchalak">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/clock"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/clock"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.UserDashboardActivity" />
        <activity android:name=".activities.AddPickUpDetailsActivity"></activity>
        <activity android:name=".activities.DriverDashboardActivity"></activity>
        <activity android:name=".activities.GetAllPickupsActivity"></activity>
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegistrationActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.EditProfileActivity" />
        <activity android:name=".MainActivity" />
       <activity android:name=".activities.MapsActivity"></activity>
        <activity android:name=".activities.DriverDetailsActivity"></activity>

        <activity android:name=".activities.UserAcceptedPickupsActivity"></activity>
        <activity android:name=".activities.DriverPickupsActivity"></activity>



        <!--SplashScreenActivity-->
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>

</manifest>